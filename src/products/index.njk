---
layout: head.njk
pageTitle: Produk
---

{% include "navbar.njk" %}

<div class="container-xxl" style="min-height: 100%;">
    <div id="products" class="container mb-4 mt-4 text-center">
        <div class="row row-cols-1 row-cols-md-2 row-cols-xl-3 gx-md-3 gy-3 text-start">
            {%- for product in collections.products | sort(attribute="data.status", reverse=true) -%}
                {% include "product-box.njk" %}
            {%- endfor -%}
        </div>
    </div>
</div>

{% include "footer.njk" %}